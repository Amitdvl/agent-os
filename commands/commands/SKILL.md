---
name: commands
description: Show the current Agent OS commands, packs, tools, skills, and readiness catalogue from manifests and installed state.
---

# Commands

Use `/commands` to run `agent-os status --catalog` from the Agent OS checkout or installed launcher. Return the generated catalogue without supplementing it from memory. Do not inspect secret values, private archives, automation prompts, or credential files.

