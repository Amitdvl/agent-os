{
  "name": "agent-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "agent-os": "./bin/agent-os"
  },
  "scripts": {
    "validate": "node bootstrap/cli.mjs validate",
    "test": "node --experimental-strip-types --test skills/skill-cleaner/scripts/skill-cleaner.test.ts tests/*.test.mjs && PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s templates/hooks/ctx7-guard/tests -p 'test_*.py'",
    "test:integration": "node --test tests/integration.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
