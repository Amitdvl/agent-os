{
  "name": "agent-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "agent-os": "./bin/agent-os"
  },
  "scripts": {
    "validate": "node bootstrap/cli.mjs validate",
    "test": "node --experimental-strip-types --test skills/skill-cleaner/scripts/skill-cleaner.test.ts tests/*.test.mjs && node scripts/run-python-tests.mjs",
    "test:integration": "node --test tests/integration.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
