{
  "name": "agent-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "agent-os": "./bin/agent-os"
  },
  "scripts": {
    "validate": "node bootstrap/cli.mjs validate",
    "test": "node --test tests/*.test.mjs",
    "test:integration": "node --test tests/integration.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}

