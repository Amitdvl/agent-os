# `strict-portable`

This default Agent OS profile selects every capability pack and preserves portable autonomy, focus, memory, freshness, secret-safety, and exact-external-action rules.

Setup always requires the new user's own local identity, paths, tools, encrypted vault, permissions, and account logins. No private machine state is bundled.
