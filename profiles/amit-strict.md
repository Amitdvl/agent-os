# `amit-strict`

This is the default Agent OS profile. It selects every capability pack and preserves Amit's portable autonomy, focus, memory, freshness, secret-safety, and exact-external-action rules.

Another user may adopt it unchanged, but setup still requires their own local identity, paths, tools, encrypted vault, permissions, and account logins. No private machine state is bundled.

