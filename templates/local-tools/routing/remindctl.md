# Apple Reminders routing requirements

- Apple Reminders: use `remindctl` for Apple Reminders list, search, create, update, complete, delete, and scheduling tasks. When creating a reminder, set its priority to high (urgent) by default unless the user explicitly requests another priority.
