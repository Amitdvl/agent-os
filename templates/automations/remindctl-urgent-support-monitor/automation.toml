version = 1
id = "{{automation_id}}"
kind = "heartbeat"
name = "Monitor remindctl Native Urgent Support"
prompt = """Monitor https://github.com/openclaw/remindctl/issues/79 for meaningful maintainer comments, label or status changes, and verified native Urgent support. Stay quiet while the issue is unchanged or non-actionable.

Do not treat a closed issue, documentation-only change, alarm help improvement, bot activity, or a statement that public EventKit lacks native Urgent as approval or implementation. Only act when maintainers explicitly approve a supported native Urgent capability or when merged/released upstream code provides it. Verify the exact option, behavior, and release/source evidence first.

After that verification, update the configured live remindctl SKILL.md and routing.md so the verified urgent option is documented and becomes the default. Mirror only the portable contract into the configured Agent OS checkout, then run its validation, full tests, git diff --check, and twin audit. Commit only the intended Agent OS mirror and push it to its configured origin. Use the configured encrypted-vault workflow if credentials are needed; never expose secret material.
"""
status = "PAUSED"
rrule = "{{rrule}}"
target_thread_id = "{{target_thread_id}}"
